# SAFX Editor — Dependências
# Instale com: pip install -r requirements.txt

# Interface gráfica
PyQt6>=6.4.0

# API REST embutida (opcional — necessário para habilitar API REST)
flask>=3.0.0
flask-cors>=4.0.0

# SFTP (opcional — necessário para envio via SFTP)
paramiko>=3.0.0

# Oracle (importação ERP / banco externo — instale pelo menos um)
# Modo thin (recomendado no Windows, sem Oracle Instant Client na maioria dos casos):
#   pip install oracledb
# Alternativa clássica (muitas vezes exige Oracle Instant Client no PATH):
#   pip install cx_Oracle

# Importação de planilhas Excel (JOIN com dados externos)
openpyxl>=3.1.0

# Utilitários (já incluídos no Python padrão)
# pathlib, json, threading, logging, sqlite3, tempfile, subprocess
